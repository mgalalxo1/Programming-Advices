#include <iostream>
#include <cmath>
using namespace std;

void PrintRectangleAreaByDiagonal(double a, double d) {
    double area = a * sqrt((d * d) - (a * a));
    cout << area;
}

int main() {
    double a, d;
    cin >> a >> d;
    PrintRectangleAreaByDiagonal(a, d);
    return 0;
}
