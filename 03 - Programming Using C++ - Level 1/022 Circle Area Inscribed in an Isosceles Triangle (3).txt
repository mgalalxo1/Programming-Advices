#include <iostream>
using namespace std;

void PrintInscribedCircleArea(double a, double b) {
    double area = (3.14 * b * b / 4) * ((2 * a - b) / (2 * a + b));
    cout << area;
}

int main() {
    double a, b;
    cin >> a >> b;
    PrintInscribedCircleArea(a, b);
    return 0;
}
