#include <iostream>
using namespace std;

int main() {
    int age;
    cin >> age;
    if (age >= 18 && age <= 45)
        cout << "Valid Age";
    else
        cout << "Invalid Age";
    return 0;
}
