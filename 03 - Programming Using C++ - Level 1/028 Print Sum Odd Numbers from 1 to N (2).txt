#include <iostream>
using namespace std;

int main() {
    int n, counter = 1, sum = 0;
    cin >> n;

    while (counter <= n) {
        if (counter % 2 != 0)
            sum += counter;
        counter++;
    }

    cout << sum;
    return 0;
}
