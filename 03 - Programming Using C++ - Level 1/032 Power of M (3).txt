#include <iostream>
using namespace std;

void PrintPower(int num, int m) {
    int p = 1;
    int counter = 0;

    while (counter < m) {
        p *= num;
        counter++;
    }

    cout << p;
}

int main() {
    int num, m;
    cin >> num >> m;
    PrintPower(num, m);
    return 0;
}
